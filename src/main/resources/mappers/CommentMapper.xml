<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pagoda.matchmeal.mapper.CommentMapper">

    <resultMap id="CommentResultMap" type="com.pagoda.matchmeal.model.entity.Comment">
        <id property="commentId" column="comment_id"/>
        <result property="userId" column="user_id"/>
        <result property="postId" column="post_id"/>
        <result property="content" column="content"/>
        <result property="parentCommentId" column="parent_comment_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>

    </resultMap>

    <resultMap id="CommentDtoMap" type="com.pagoda.matchmeal.model.dto.response.CommentResponseDto">
        <id property="commentId" column="comment_id"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="parentCommentId" column="parent_comment_id"/>
        <result property="likeCount" column="like_count"/>

        <association property="user" javaType="com.pagoda.matchmeal.model.dto.UserSimpleDto">
            <id property="userId" column="user_id"/>
            <result property="userName" column="user_name"/>
            <result property="profileImage" column="profile_image"/>
        </association>
    </resultMap>

    <insert id="save" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO comments (post_id, user_id, content, parent_comment_id, created_at, updated_at)
        VALUES (#{postId}, #{userId}, #{content}, #{parentCommentId}, NOW(), NOW())
    </insert>

    <select id="findAllByPostId" resultMap="CommentDtoMap">
        SELECT c.comment_id,
               c.post_id,
               c.content,
               c.parent_comment_id,
               c.created_at,
               c.updated_at,
               c.deleted_at,

               -- 작성자 정보
               u.user_id,
               u.user_name,
               u.profile_image,

               -- 좋아요 개수 (서브쿼리)
               (SELECT COUNT(*) FROM comment_likes cl WHERE cl.comment_id = c.comment_id) AS like_count

        FROM comments c
                 JOIN users u ON c.user_id = u.user_id
        WHERE c.post_id = #{postId}
          -- 삭제된 댓글도 대댓글이 있으면 '삭제된 댓글입니다'로 보여줘야 하므로 
          -- 일단 다 가져오거나, 정책에 따라 AND c.deleted_at IS NULL 추가
          AND c.deleted_at IS NULL

        ORDER BY
            -- 1차 정렬: 부모 댓글 ID (부모가 null이면 자기 자신 ID 사용) -> 댓글 그룹핑
            COALESCE(c.parent_comment_id, c.comment_id) ASC,
            -- 2차 정렬: 부모는 먼저, 자식은 나중에 (오름차순)
            c.created_at ASC
    </select>

    <select id="findByCommentId" resultMap="CommentDtoMap">
        SELECT c.comment_id,
               c.post_id,
               c.content,
               c.parent_comment_id,
               c.created_at,
               c.updated_at,

               -- 작성자 정보 (User)
               u.user_id,
               u.user_name,
               u.profile_image,

               -- 좋아요 개수
               (SELECT COUNT(*) FROM comment_likes cl WHERE cl.comment_id = c.comment_id) AS like_count

        FROM comments c
                 JOIN users u ON c.user_id = u.user_id
        WHERE c.comment_id = #{commentId}
    </select>

    <update id="update">
        UPDATE comments
        SET content    = #{content},
            updated_at = NOW()
        WHERE comment_id = #{commentId}
    </update>

    <update id="delete">
        UPDATE comments
        SET deleted_at = NOW()
        WHERE comment_id = #{commentId}
    </update>

    <select id="countByPostId" resultType="int">
        SELECT COUNT(*)
        FROM comments
        WHERE post_id = #{postId}
          AND deleted_at IS NULL
    </select>

    <select id="existsLike" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM comment_likes
        WHERE comment_id = #{commentId}
          AND user_id = #{userId}
    </select>

    <insert id="insertLike">
        INSERT INTO comment_likes (comment_id, user_id)
        VALUES (#{commentId}, #{userId})
    </insert>

    <delete id="deleteLike">
        DELETE
        FROM comment_likes
        WHERE comment_id = #{commentId}
          AND user_id = #{userId}
    </delete>

</mapper>