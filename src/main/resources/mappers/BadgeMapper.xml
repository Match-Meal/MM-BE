<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pagoda.matchmeal.mapper.BadgeMapper">

    <select id="findAllByOrderByTierAsc" resultType="com.pagoda.matchmeal.model.entity.Badge">
        SELECT * FROM badges ORDER BY tier ASC
    </select>

    <select id="findAll" resultType="com.pagoda.matchmeal.model.entity.Badge">
        SELECT * FROM badges
    </select>

</mapper>
